# AUTH-DESIGN.md — 身份驗證與權限架構設計文件

> 本文件因 BUG-005（登入/登出/角色權限缺口）而產生。目的是先釐清現況、架構限制與可行方案，
> **不在此文件中片面決定商業規則**（例如「店員不能打折」是否成立），凡屬商業規則的項目都標記
> `NEED BUSINESS DECISION` 並列出選項，待店家/負責人確認後才會實作對應的程式邏輯。

---

## 1. 目前使用者資料結構

檔案：`js/db.js`（`seed()` 內 `users` 陣列）

```js
users: [
  { id: 'U-xxxx', name: '店長', role: 'admin', pin: '0000' }
]
```

透過 `js/settings.js` 的「人員管理」可以新增/編輯人員，欄位為：
- `name`（姓名，必填）
- `role`（`admin` 管理員 / `staff` 店員）
- `pin`（新增人員時固定寫死 `'0000'`，UI 上完全沒有任何欄位可以修改它）

## 2. `users` 結構的現況問題

1. `users` 陣列本身只是「資料」，**沒有任何程式碼會去讀取「目前是誰在操作」**。
2. POS 結帳寫入訂單時，`cashier` 欄位固定寫死 `s.users[0].name`（陣列中第一筆使用者，也就是預設種子資料的「店長」），與畫面上是否新增了其他人員完全無關。
3. 側邊欄固定顯示文字「店長．已登入」（`index.html` 寫死的靜態文字），不是從 `users` 資料算出來的。
4. 沒有「目前登入者」這個概念（沒有 `currentUser`、沒有 session、沒有 localStorage 記錄「誰在用這台裝置」）。

## 3. `role` 欄位

目前只有兩個值：`admin`（管理員）／`staff`（店員）。**除了在「人員管理」清單顯示中文字「管理員/店員」之外，`role` 沒有被其他任何程式碼讀取或用來做判斷**（已用 `grep` 全專案確認，只有 `settings.js` 顯示用途）。

## 4. `pin` 欄位

資料結構已預留 `pin` 欄位（新增人員時固定給 `'0000'`），但：
- 沒有登入畫面去要求輸入 PIN
- 沒有任何 UI 可以「修改」某人的 PIN
- 沒有任何程式碼讀取/比對這個欄位

也就是說，`pin` 目前是一個完全未使用的「預留欄位」。

## 5. 目前系統內有哪些「頁面」

| 頁面 | 說明 |
|---|---|
| 營運總覽 | 儀表板 |
| POS 收銀 | 銷售、折扣、多元付款 |
| 訂單管理 | 美容/寄養/安親 訂單 + 服務項目設定 |
| 美容教學課程 | 課程管理 + 報名管理 |
| 交易紀錄查詢 | 查詢、退款、作廢 |
| 商品管理 | 商品 CRUD + 分類管理 |
| 進貨管理 | 供應商 + 進貨單 + 入庫 |
| 庫存管理 | 即時庫存、異動紀錄、盤點、效期管理 |
| 報表 | 營收/毛利報表 |
| 設定 | 店家資訊、人員管理、資料備份 |

目前**全部頁面對任何人開放**，沒有任何一頁會因為角色不同而隱藏或鎖定。

## 6-8. 哪些操作應該限制角色／需要管理員授權／需要二次驗證

> ⚠️ **NEED BUSINESS DECISION** — 以下只是根據一般零售/寵物業 POS 常見做法列出的「建議選項」，
> 實際規則必須由店家決定，本次不會自行實作成強制規則。

| 操作 | 選項 A（較嚴格） | 選項 B（較寬鬆） | 選項 C |
|---|---|---|---|
| 刪除商品 | 僅管理員 | 店員也可以 | — |
| 庫存人工調整（盤盈/盤虧/報廢等） | 僅管理員 | 店員可調整但需備註 | 店員調整需管理員 PIN 覆核 |
| 退款 | 僅管理員 | 店員可退款但會被記錄「操作者」供事後稽核 | 需管理員 PIN 覆核才能執行 |
| 作廢訂單 | 同退款 | 同退款 | 同退款 |
| 高於一定比例的折扣 | 需管理員 PIN 覆核 | 店員可自由打折 | 系統只記錄不阻擋（見 BUG-009） |
| 人員管理／新增人員 | 僅管理員 | — | — |
| 報表查詢 | 兩者皆可看 | 僅管理員能看毛利/成本，店員只能看營收 | — |
| 商品新增/編輯 | 兩者皆可 | 僅管理員 | — |
| 進貨/供應商管理 | 兩者皆可 | 僅管理員 | — |

**在店家確認以上規則之前，本次修正只會實作「技術上可以支援未來限制」的骨架（見下方第一階段），不會實際擋下任何店員的操作。**

## 9. 目前 localStorage 架構的限制

這是純前端、無後端、無網路同步的架構，任何身份驗證機制都受以下限制：

1. **PIN 碼不是真正的安全機制**：所有資料（含 PIN）都明文存在瀏覽器的 `localStorage`，任何打開瀏覽器開發者工具的人都可以直接讀取或竄改。這只能做到「操作紀錄歸屬」與「防止誤觸」，**無法防止真正有心人繞過**。
2. **沒有伺服器驗證**：無法做到真正的「登入權杖 (token)」機制，登入狀態只能存在單一裝置的 `localStorage`，換裝置或清除瀏覽器資料就會登出。
3. **多裝置/多店員同時使用會衝突**：目前資料是單機 `localStorage`，如果同一家店有兩台平板，兩邊的資料不會同步，也無法共用同一套「目前有誰登入」的狀態。
4. **無法做到金鑰/密碼雜湊**：前端 JS 沒有安全的方式儲存密碼雜湊而不被讀取，PIN 碼只適合當作「快速身份確認」，不適合當作真正密碼使用。

## 10. 未來如果接 Firebase / Backend，建議怎麼升級

1. **帳號驗證**：改用 Firebase Authentication（Email/密碼或匿名+PIN 綁定），或自建後端 API + JWT。
2. **資料儲存**：`DB.state` 目前直接對應 `localStorage`，可以抽換成 Firestore/後端 API，讀寫介面（`DB.save()`/`DB.state`）已經是唯一入口，抽換時只需改 `js/db.js`，其餘模組不需大改。
3. **權限**：伺服器端（Firestore Security Rules 或後端 API 中介層）強制驗證角色，前端的角色檢查只能當作 UX 提示，**真正的權限控管必須在伺服器端**，否則跟現在一樣可被繞過。
4. **多裝置同步**：改用雲端資料庫後，多台平板可以共用即時庫存/訂單資料，需注意併發寫入（例如兩台平板同時賣出最後一件庫存）的競爭條件處理。
5. **稽核紀錄**：目前已在下方「第一階段」加入 `cashier`/`operator` 欄位的骨架，未來接後端時可以延伸為完整的操作稽核紀錄（誰、何時、做了什麼、IP、裝置）。

---

## 實作階段規劃

### 第一階段（本次執行）—— 導入 `currentUser` 概念，移除寫死的 `s.users[0]`

**不做**：登入畫面、PIN 驗證、登出、角色限制（這些留到店家確認規則後的第二/三階段）。

**要做**：
1. 在 `DB` 增加 `currentUser` 概念（存於 `localStorage`，初始值 = `users[0]`）。
2. 側邊欄「操作者」下拉選單，可以從既有人員清單中「切換目前操作者」（不需密碼，僅供歸屬用，避免無意義地卡住店家日常操作）。
3. POS 結帳 `cashier` 欄位、庫存調整、退款/作廢等操作，記錄的「操作者」一律改讀 `DB.currentUser.name`，不再寫死 `s.users[0].name`。
4. 側邊欄文字「店長．已登入」改為即時顯示 `目前操作者`。

此階段的效益：至少交易紀錄能正確反映「誰在操作」，且已為第二/三階段的登入/權限機制預留好資料串接的位置，不需要之後再大改一次資料流。

### 第二階段（待需求確認後）—— 登入 / PIN / 登出 / 身份切換
需要店家確認：是否要用 PIN 碼登入、PIN 是否要能自訂、是否要有登入逾時自動登出。

### 第三階段（待業務規則確認後）—— 權限限制
依上方「6-8」確認的規則表，在對應功能加上角色檢查與/或管理員 PIN 覆核彈窗。
