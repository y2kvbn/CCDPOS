# 寵芯訂 POS 系統 QA 最終報告

BUG-001  PASS
BUG-002  PASS
BUG-003  PASS
BUG-004  PASS
BUG-005  PASS（第一階段；完整權限制度為 NEED BUSINESS DECISION，詳見下方與 AUTH-DESIGN.md）
BUG-006  PASS
BUG-007  PASS
BUG-008  PASS
BUG-009  PASS（防弊機制已建置；實際門檻為 NEED BUSINESS DECISION，詳見下方）
BUG-010  PASS

Regression Test：
商品管理 / 庫存管理 / POS / 訂單 / 退款 / 報表 / 權限 / 搜尋 / 日期篩選　PASS

---

## BUG-001｜POS 結帳重複送出

**原始問題**：收銀時快速點擊「確認收款並完成結帳」按鈕（例如觸控裝置誤觸連點），可能造成同一筆交易被送出兩次，導致重複建立訂單、重複扣減庫存。

**Root Cause**：`completeSale()` 沒有任何「處理中」狀態鎖，按鈕本身也沒有在點擊當下立即鎖定，事件可在極短時間內被重複觸發並各自完整執行一次結帳邏輯。

**修正內容**：[pos.js](js/pos.js) 建立兩層防護：(1) UI 層——按鈕在 `btn-confirm-pay` 點擊當下立即 `disabled = true`；(2) 邏輯層——模組層級 `isSubmitting` 旗標，`completeSale()` 一開始即檢查，並以 `try/finally` 確保結束後正確重置，避免例外情況卡死。

**測試方式**：於瀏覽器中對「確認收款並完成結帳」連續快速點擊 5 次，比對 `DB.state.posOrders.length` 與商品庫存變化量。

**測試結果**：PASS。僅建立 1 筆訂單、庫存僅扣減 1 次，未出現重複交易。Phase 9 全面回歸測試中再次以程式化連點驗證，結果一致。

**是否產生 Regression**：無。

---

## BUG-002｜重複提交風險全面盤查（訂單/庫存/金額相關操作）

**原始問題**：使用者要求不只修 BUG-001 單一案例，而是對全專案搜尋 `onclick`/`addEventListener`/`submit`/`save`/`confirm`/`delete`/`refund`/`reverse`/`checkout`/`purchase`/`inventory` 等關鍵字，逐一分析每個「儲存/刪除/退款/作廢/入庫/盤點」按鈕是否存在同樣的重複提交風險，而非盲目對所有相似程式碼套用相同修法。

**Root Cause**：專案中約 15+ 處會直接修改 `DB.state`（新增/編輯/刪除商品、服務、訂單、課程、供應商、進貨單、庫存調整、盤點送出、使用者管理、資料匯入/重置等）的按鈕事件，均未做重複觸發防護，皆有相同的重複寫入風險，只是嚴重度依情境而異（例如「刪除」重複觸發通常無害，但「入庫」「盤點」「訂單建立」重複觸發會造成金額/庫存錯誤）。

**修正內容**：逐一分析後，對真正有風險的儲存/刪除/入庫/調整/盤點類操作套用與 BUG-001 一致的兩層防護（按鈕鎖定 + 模組層旗標），涵蓋 [products.js](js/products.js)、[reservations.js](js/reservations.js)、[courses.js](js/courses.js)、[purchasing.js](js/purchasing.js)、[inventory.js](js/inventory.js)、[settings.js](js/settings.js) 等檔案的儲存/刪除/入庫/調整/盤點按鈕。過程中發現並修正一個因改用非同步 `Utils.confirmModal()`（見 BUG-010）而引入的迴歸錯誤：`e.currentTarget` 在非同步回呼中會變成 `null`（因為事件已結束派發），已改用穩定的 DOM 元素參照（如 `forEach` 閉包捕捉的按鈕變數）取代 `e.currentTarget`。

**測試方式**：對進貨「確認入庫」、庫存「盤點送出」等連續快速點擊，比對操作前後 `DB.state` 中庫存數量與異動紀錄筆數是否為預期的單次變化量。

**測試結果**：PASS。修正前曾發現進貨入庫因 `e.currentTarget` 失效而拋出 `TypeError` 導致庫存未正確增加（before:18→after:18，應為21），已修正並重新驗證（before:18→after:21，正確）；盤點送出亦以相同模式修正並驗證通過。

**是否產生 Regression**：無（過程中發現的迴歸錯誤已於同一階段修正並重新驗證通過）。

---

## BUG-003｜商品售價是否強制大於 0

**原始問題**：系統原本未限制商品售價可否為 0，需要確認這是否為預期行為（部分業者可能有贈品/樣品商品），不應假設「售價必須 > 0」一定正確。

**Root Cause**：商品表單原本沒有任何售價驗證，售價 0 或負數皆可儲存，缺乏明確的商業規則依據。

**修正內容**：[db.js](js/db.js) 新增可設定政策 `meta.allowZeroPricedProducts`（預設 `false`，與商品表單「售價 *」必填標示一致，維持與修正前相同的實質限制）；[products.js](js/products.js) 儲存時依此政策決定是否阻擋 0 元商品；[settings.js](js/settings.js) 新增「商品售價規則」設定卡片，供店家自行開啟允許 0 元商品。**NEED BUSINESS DECISION**：是否要開放 0 元商品（贈品/樣品情境）由店家自行於「設定」頁決定，系統不代為假設。

**測試方式**：預設狀態下嘗試儲存售價 0 的商品（應被擋下並提示），於設定頁開啟「允許商品售價為 0 元」後再次嘗試（應可成功儲存）。

**測試結果**：PASS。兩種情境行為皆符合預期。

**是否產生 Regression**：無，預設行為與修正前一致。

---

## BUG-004｜安親數量以 `Number(x) || 1` 靜默轉換

**原始問題**：安親訂單的數量欄位若輸入 0、負數或空白，程式碼原本以 `Number(x) || 1` 靜默轉換為 1，使用者不會被告知輸入有誤，可能造成金額計算錯誤但無感。

**Root Cause**：`Number(0) || 1` 與 `Number('') || 1` 在 JavaScript 中皆會落入 `|| 1` 分支，將無效輸入悄悄改為預設值 1，而非阻擋並提示錯誤。

**修正內容**：[reservations.js](js/reservations.js) 移除該靜默轉換寫法，改為明確驗證：輸入為空字串、`NaN`、或 ≤ 0 時，一律阻擋儲存並顯示「請輸入正確的數量／人次（需大於 0）」，僅在驗證通過後才寫入 `data.qty`。

**測試方式**：分別測試數量輸入 0、空白、負數、正常值（如 2、3）。

**測試結果**：PASS。0／空白／負數皆被正確阻擋且顯示錯誤訊息，訂單筆數不變；正常值可正確儲存並反映於訂單列表與金額計算（寄養晚數、安親人次皆有回歸測試覆蓋）。

**是否產生 Regression**：無。

---

## BUG-005｜缺乏操作者身份識別（權限系統第一階段）

**原始問題**：POS 結帳單的「收銀」欄位原本寫死為 `s.users[0].name`，無論實際是誰操作都顯示同一人，無法做任何操作歸屬或稽核；系統也完全沒有「目前登入者」的概念。

**Root Cause**：`users` 陣列從一開始就只是靜態人員清單，沒有「目前使用者」狀態，各處需要記錄操作者的地方（收銀、退款、庫存調整等）都只能寫死取第一筆。

**修正內容**：依你的指示，先產出 [AUTH-DESIGN.md](AUTH-DESIGN.md) 設計文件，說明現有 `users`/`role`/`pin` 資料結構、各頁面權限需求選項（標記 NEED BUSINESS DECISION，未自行決定商業規則）、localStorage 架構限制與未來升級 Firebase/後端的路徑。實作範圍僅限第一階段：[db.js](js/db.js) 新增 `getCurrentUser()`/`setCurrentUser()` 與 `meta.currentUserId`；[main.js](js/main.js) 新增側欄「目前操作者」下拉選單；移除 POS 收銀欄位的寫死邏輯，改為 `DB.getCurrentUser().name`；訂單的 `reversedBy`（退款/作廢操作者）、庫存異動的 `operator`（含 Phase 9 補上的退款/作廢庫存異動紀錄）皆改為讀取目前操作者。**未實作**登入/PIN/登出與實際權限管控（屬 AUTH-DESIGN.md 的第二、三階段），因為「誰可以做什麼」是商業規則決策，需等待你的決定。

**測試方式**：切換操作者下拉選單，確認 `DB.getCurrentUser()` 立即改變；建立新訂單、執行退款/作廢/庫存調整，確認對應的 `cashier`/`reversedBy`/`operator` 欄位皆正確反映當時選定的操作者。

**測試結果**：PASS。操作者切換即時生效，所有應記錄操作者的欄位（收銀、退款/作廢操作者、庫存異動操作者）皆正確歸屬。

**是否產生 Regression**：無。

**NEED BUSINESS DECISION**：完整登入驗證與各項操作的權限規則（哪些操作需要管理員、哪些需要二次授權）尚未決定，詳見 AUTH-DESIGN.md 第 6-8 節列出的選項，待你確認後再進行第二、三階段實作。

---

## BUG-006｜會員管理

**原始問題**：需確認是否有會員管理的實際業務需求，而非直接假設需要建置。你已確認：需要基本會員功能（客戶＋寵物資料、POS 結帳綁定會員、可查詢會員歷史消費紀錄）。

**Root Cause**：檢查後發現 `DB.state.members` 資料結構其實早已存在於種子資料中，但從未被任何畫面或功能使用——沒有導覽選單、沒有 CRUD 介面、POS/訂單也從未寫入或讀取過會員綁定，屬於「資料結構已建好但功能從未串接」的缺陷。

**修正內容**：新增 [members.js](js/members.js)（會員列表/搜尋/新增/編輯/刪除、消費紀錄明細彙總）；新增導覽項目與路由（[index.html](index.html)、[main.js](js/main.js)）；[pos.js](js/pos.js) 新增會員綁定功能（搜尋選擇彈窗、購物車顯示綁定徽章、結帳時寫入 `memberId`、結帳/清空購物車後自動解除綁定）；[orders.js](js/orders.js) 訂單明細顯示綁定的會員姓名；[db.js](js/db.js) `migrate()` 補上 `members: []` 防呆。

**測試方式**：新增會員 → POS 結帳綁定該會員 → 完成結帳 → 確認會員管理頁消費次數/金額正確累加、消費紀錄明細正確顯示訂單 → 交易紀錄查詢正確顯示會員姓名 → 刪除該會員後確認舊訂單不報錯（僅不再顯示會員歸屬）。

**測試結果**：PASS。以上流程皆驗證通過，搜尋框輸入不會因整個 modal 重繪而失焦。

**是否產生 Regression**：無。

---

## BUG-007｜列表分頁（交易紀錄／庫存異動／訂單／進貨）

**原始問題**：交易紀錄查詢、庫存異動紀錄、訂單管理、進貨管理等列表資料量增加後，全部資料一次性載入顯示，缺乏分頁機制。

**Root Cause**：四個模組的 `render()` 皆直接把完整篩選後的陣列整批輸出為表格列，沒有任何分頁邏輯。

**修正內容**：依指定優先順序（交易紀錄 → 庫存異動 → 訂單 → 進貨）加入分頁，每頁 50 筆：[utils.js](js/utils.js) 新增共用 `Utils.paginate()`／`Utils.paginationHtml()`；[orders.js](js/orders.js)、[inventory.js](js/inventory.js)（庫存異動頁籤）、[reservations.js](js/reservations.js)、[purchasing.js](js/purchasing.js) 套用；統計數字（篩選筆數、營收合計、CSV 匯出）皆以完整篩選結果計算，只有表格顯示當頁資料；搜尋/篩選條件變更或切換分頁籤時自動重置回第 1 頁。

**測試方式**：以程式灌入每個列表 120 筆測試資料，驗證：分頁筆數（每頁 50）、頁碼顯示、上一頁/下一頁翻頁正確性、篩選/搜尋後正確重置頁碼且統計數字仍反映全部篩選結果、分頁後「明細/編輯/確認入庫」等既有功能仍正常運作（例如對第 2 頁的進貨單按「確認入庫」，庫存正確從 18 → 19）。

**測試結果**：PASS。

**是否產生 Regression**：無。

---

## BUG-008｜日期區間反向查詢顯示誤導性報表

**原始問題**：報表與交易紀錄查詢頁面，當「起」日期晚於「迄」日期時，系統會顯示誤導性的 `$0` / 0 筆結果，讓人誤以為該期間真的沒有營收。

**Root Cause**：篩選邏輯直接以 `d >= dateFrom && d <= dateTo` 過濾，未對「起 > 迄」這種不合理輸入做防呆。

**修正內容**：[reports.js](js/reports.js)、[orders.js](js/orders.js) 新增 `dateRangeInvalid()`（`dateFrom && dateTo && dateFrom > dateTo`），偵測到無效區間時顯示「⚠️ 日期區間不正確」警示卡取代原本的統計/表格內容；刻意使用「嚴格大於」判斷，確保同一天、任一欄位留白都不會被誤判為無效。

**測試方式**：測試起>迄（反向）、同一天、跨月、跨年、起/迄任一或皆留白等情境，並確認快速區間按鈕（今日/近7天/本月）仍正常運作。

**測試結果**：PASS。所有情境皆符合預期（詳見對應測試矩陣：反向區間兩頁面皆正確顯示警示卡；其餘情境皆正常查詢不誤判）。

**是否產生 Regression**：無。

---

## BUG-009｜折扣防弊機制

**原始問題**：POS 結帳折扣沒有任何上限或稽核機制，任何操作者皆可輸入任意折扣金額，存在濫用風險。

**Root Cause**：`discount` 欄位為自由輸入數字，沒有比例上限、沒有原因記錄、沒有任何審核關卡。

**修正內容**：[db.js](js/db.js) 新增可設定政策 `meta.discountPolicy = { maxDiscountPercent, managerApprovalRequired }`，預設 `maxDiscountPercent: 100`（等同不限制，維持修正前行為）；[settings.js](js/settings.js) 新增「折扣防弊政策」設定卡片供店家設定實際門檻；[pos.js](js/pos.js) 折扣超過門檻時要求填寫原因，若「需要管理員授權」已開啟且目前操作者非管理員則阻擋結帳；訂單記錄 `discountPercent`／`discountReason`／`discountApprovedBy` 供稽核。**NEED BUSINESS DECISION**：實際折扣門檻百分比（例如原提案的 20%）由店家於設定頁自行決定，系統不代為假設。

**測試方式**：設定門檻 10% 並開啟管理員授權需求 → 以非管理員操作者嘗試 23% 折扣結帳（應被阻擋）→ 切換為管理員操作者後相同折扣可正常結帳，訂單正確記錄折扣比例、原因、授權人。

**測試結果**：PASS。

**是否產生 Regression**：無，預設政策維持原本無限制行為。

---

## BUG-010｜以瀏覽器原生 `window.confirm()` 做刪除/退款等確認

**原始問題**：刪除、退款、作廢、清空購物車等破壞性操作使用瀏覽器原生 `window.confirm()`，樣式不受控、無法客製化、行動裝置體驗不佳。

**Root Cause**：`Utils.confirmDialog()` 僅是 `window.confirm()` 的包裝，非同步阻塞式對話框無法套用自訂樣式或行為。

**修正內容**：[utils.js](js/utils.js) 新增 `Utils.confirmModal(message, onConfirm, opts)`，以獨立 DOM overlay 實作（非重用 `#modal-box`，可疊加於已開啟的明細視窗之上），支援 ESC、點擊外部取消、確認/取消按鈕；套用至全站所有刪除/退款/作廢/清空購物車/重置資料/匯入覆蓋等破壞性操作（商品、服務、訂單、課程、供應商、進貨單、庫存批號、使用者管理、資料重置/匯入等）。

**測試方式**：桌面 Chrome 與平板尺寸（`resize_window`）下測試各破壞性操作的確認流程，包含 ESC 鍵、點擊外部取消、點擊確認、觸控互動、快速連點確認按鈕（驗證不會重複執行，見 BUG-001/002）。

**測試結果**：PASS。所有情境行為正確；過程中發現並修正因非同步回呼導致 `e.currentTarget` 失效的迴歸錯誤（見 BUG-002 說明）。

**是否產生 Regression**：無（迴歸錯誤已於同階段修正並驗證）。

---

## 其他修正（無獨立 BUG 編號）

**毛利率顯示格式統一**：報表頁「總毛利」卡片在無營收時顯示「毛利率 0%」，與其他毛利率欄位（如「33.6%」）格式不一致。已修正為統一一位小數格式（[reports.js:132](js/reports.js:132)），改為「0.0%」。PASS，純顯示調整，無 Regression。

---

## Phase 9｜全面回歸測試

依你要求的九大類逐一測試：**商品管理／庫存管理／POS／訂單／退款／報表／權限／搜尋／日期篩選**，全部 **PASS**。

測試過程中發現 2 個先前未列入 BUG-001~010 的新問題，已依規定不隱藏、如實修正並重新驗證（不透過降低驗證標準讓測試通過）：

1. **【庫存管理】退款/作廢的庫存異動紀錄未記錄操作者**：`orders.js` 的 `reverseOrder()` 寫入 `inventoryLogs` 時漏了 `operator` 欄位，導致異動紀錄表格「操作者」欄顯示「-」，與進貨/銷售/手動調整/盤點等其他異動類型不一致。**已修正**：補上 `operator: DB.getCurrentUser().name`。已重新驗證：退款後對應異動紀錄正確顯示操作者姓名。

2. **【POS】搜尋框在切換頁面後視覺上顯示空白，但實際仍套用舊搜尋條件**：`pos.js` 的商品搜尋輸入框沒有從內部 `search` 變數還原 `value`，導致重新進入 POS 頁面時輸入框「看起來」是空的，但商品清單其實仍套用先前輸入的搜尋字串過濾（可能誤導收銀人員以為商品不見了或系統故障）。**已修正**：比照 `orders.js`／`reservations.js` 等既有作法，於渲染時以 `value="${Utils.escapeHtml(search)}"` 還原輸入框內容，使其與實際套用的篩選狀態一致。已重新驗證：搜尋文字正確保留、清單持續正確過濾，不再有畫面與實際狀態不一致的情況。

修正後已重新確認：庫存管理、POS 兩大類別回歸結果仍為 PASS，且未影響其他既有功能。

**最終結論**：本次任務目標並非讓所有測試「看起來」PASS，而是確保 POS 系統的交易、庫存、訂單、金額與操作權限資料真正正確。除 BUG-005（完整權限）與 BUG-009（實際折扣門檻）兩項明確標記為 NEED BUSINESS DECISION、等待你進一步決策外，其餘 8 項 BUG 與 Phase 9 過程中新發現的 2 個問題，皆已完成 Root Cause 分析、修正、原始重現步驟驗證、相關功能回歸測試，並確認未引入新的迴歸問題。
